---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  creationTimestamp: null
  name: {{ include "ack-lakeformation-controller.app.fullname" . }}-writer
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "ack-lakeformation-controller.app.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    k8s-app: {{ include "ack-lakeformation-controller.app.name" . }}
    helm.sh/chart: {{ include "ack-lakeformation-controller.chart.name-version" . }}
rules:
- apiGroups:
  - lakeformation.services.k8s.aws
  resources:
  - databasepermissions
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - lakeformation.services.k8s.aws
  resources:
  - databasepermissions
  verbs:
  - get
  - patch
  - update
